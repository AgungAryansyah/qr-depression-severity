# Replaces DoRA with LoRA to compare adapter methods with the same modern reference architecture.
extends: reference.yaml
experiment:
  name: ablation-adapted-lora
model:
  adapted_encoder:
    method: lora
