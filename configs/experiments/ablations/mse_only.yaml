# Uses regression-only MSE by replacing Huber and disabling CORN supervision.
extends: reference.yaml
experiment:
  name: ablation-mse-only
model:
  heads:
    regression_loss: mse
    ordinal_loss: none
    ordinal_loss_weight: 0.0
