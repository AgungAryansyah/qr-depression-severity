# Trains the adapted-only source model that initializes the warm-average stage-two run.
extends: reference.yaml
experiment:
  name: ablation-warm-stage-one
model:
  semantic_encoder:
    enabled: false
