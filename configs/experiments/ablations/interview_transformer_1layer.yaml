# Reduces the interview Transformer to one layer to measure the second layer's contribution.
extends: reference.yaml
experiment:
  name: ablation-interview-transformer-1layer
model:
  interview_encoder:
    layers: 1
