# Runs stage two of the paper-inspired warm start with average dual-branch fusion.
extends: reference.yaml
experiment:
  name: ablation-warm-average
model:
  branch_fusion:
    mode: average
training:
  initialization:
    mode: warm_start
    source_checkpoint: null
